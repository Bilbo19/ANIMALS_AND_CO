<h1> <%= @booking.first_name %>.</h1>
<%= form_for @booking, :html => { :class => "form-horizontal"} do |f| %>
  <%= f.label :first_name, "User" %>
  <%= f.text_field :first_name %>
  <%= f.submit class: 'btn btn-flat' %>
<% end %>
